<nav class="navbar navbar-expand bg-white topbar mb-4 pe-3 shadow-sm">
    <ul class="navbar-nav ms-auto">
        {% if not is_granted('IS_AUTHENTICATED_FULLY') %}
        <div class="topbar-divider d-none d-sm-block"></div>
        <li class="nav-item cs-pointer ">
            <a class="nav-link" href="{{ path('app_login') }}">
                <span class="text-primary">Login</span></a>
        </li>
        {% endif %}

        {% if not is_granted('IS_AUTHENTICATED_FULLY') %}
        <div class="topbar-divider d-none d-sm-block"></div>
        <li class="nav-item cs-pointer">
            <a class="nav-link" href="{{ path('app_register') }}">
                <span class="text-primary">Register</span></a>
        </li>

        <div class="topbar-divider d-none d-sm-block"></div>
        <li class="nav-item cs-pointer">
            <a class="nav-link" href="{{ path('app_about') }}">
                <span class="text-primary">About</span></a>
        </li>
        {% endif %}

        {% if is_granted('IS_AUTHENTICATED_FULLY') %}
        <div class="topbar-divider d-none d-sm-block"></div>
        <li class="nav-item dropdown">
            <a class="nav-link dropdown-toggle" data-bs-toggle="dropdown" href="#" role="button" aria-expanded="false"> <span class="me-1">{{ app.user.firstName }}</span>
                <i class="fa-regular fa-circle-user"></i></a>
            <ul class="dropdown-menu">
                <li><a class="dropdown-item" href="{{ path('app_logout') }}">Logout</a></li>
            </ul>
        </li>
        {% endif %}
    </ul>
</nav>